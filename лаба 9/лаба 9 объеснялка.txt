🧾 Общее объяснение

В этом коде я показываю наследование, абстрактные классы, инкапсуляцию и полиморфизм.
Каждая часть демонстрирует конкретный принцип ООП на своём примере.

💳 1. Payment — абстрактный класс и наследники

Я создал абстрактный класс Payment, в котором есть два метода — pay() и refund().
Это просто шаблон: сам класс ничего не делает, но заставляет дочерние классы реализовать эти методы.

Потом я сделал два класса:

CreditCardPayment — имитация оплаты по карте,

CryptoPayment — оплата криптовалютой.

Оба переопределяют методы и выводят свои сообщения.
В конце я создаю список из двух способов оплаты и по очереди вызываю pay() и refund().
Так я показываю полиморфизм — один и тот же метод работает по-разному в зависимости от объекта.

🎓 2. Course — абстрактный курс

Тут у меня класс Course, в котором описаны методы start(), get_materials() и end().
Это общий шаблон для любых курсов.

Дальше два наследника:

PythonCourse — курс по Python,

MathCourse — курс по математике.

Они каждый по-своему запускаются и имеют разные материалы.
Я прохожу по списку курсов, запускаю, показываю материалы и завершаю — тем самым демонстрирую,
что один интерфейс (Course) работает с разными реализациями.

🚚 3. Delivery — разные виды доставки

Абстрактный класс Delivery задаёт методы calculate_cost() и deliver().
Я реализовал три вида доставки:

AirDelivery (авиа),

GroundDelivery (наземная),

SeaDelivery (морская).

У каждой своя формула расчёта стоимости.
Я создаю список и для каждой доставки вывожу стоимость и сообщение.
Это показывает наследование + переопределение методов.

🏦 4. BankAccount — инкапсуляция

Здесь я показываю приватные поля — __owner, __balance, __pin.
Они недоступны напрямую, можно работать только через методы:

deposit() — пополнение счёта,

withdraw() — снятие денег,

change_pin() — смена PIN-кода.

В каждом методе есть проверки на правильность PIN и корректность суммы.
Так я демонстрирую защиту данных (инкапсуляцию).

👤 5. UserProfile — работа с логином и статусом

В этом классе я тоже использую приватные поля (__email, __password).
Метод login() проверяет, правильные ли введены данные.
Если вход успешен, можно вызвать upgrade_to_premium() — повысить статус с "free" до "premium".
Метод get_info() возвращает email и статус, но не показывает пароль.
Это пример ограниченного доступа к данным.

🛒 6. Product — скидки только для администратора

Класс Product хранит цену и приватное поле __discount.
Есть метод get_price(), который возвращает цену с учётом скидки.
Метод set_discount() можно вызвать только если is_admin=True,
иначе выдаёт ошибку доступа.
Это демонстрация контроля прав и скрытых данных.

🗂 7. TextFile, ImageFile, AudioFile — полиморфизм

У меня есть три разных типа файлов.
Каждый имеет метод open(), но поведение у всех разное.
Функция open_all(files) принимает список и вызывает open() у каждого.
Полиморфизм снова: один вызов — разное поведение в зависимости от типа объекта.

🚗 8. Car, Truck, Bicycle — симуляция транспорта

Каждый класс (Car, Truck, Bicycle) имеет метод move(distance).
Я рассчитываю время пути в зависимости от скорости.
Функция simulate_transport() принимает список транспорта и вызывает move() для всех.
Это пример общего интерфейса для разных объектов.

🧑‍🎓 9. Student, Teacher, Administrator — уровни доступа

Здесь у всех классов есть метод access_portal(),
но каждый выводит свои права:

студент видит расписание,

преподаватель ставит оценки,

администратор управляет системой.

Я создаю список и вызываю access_portal() у каждого,
чтобы показать, как один и тот же метод реализуется по-разному.

⚙️ Итого

Всё вместе этот код демонстрирует:

Наследование (общий класс → специализированные),

Полиморфизм (один метод — разное поведение),

Инкапсуляцию (приватные и защищённые данные),

Абстракцию (работаем через интерфейсы, не зная деталей реализации).